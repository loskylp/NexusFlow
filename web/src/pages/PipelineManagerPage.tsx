/**
 * PipelineManagerPage — placeholder for the Pipeline Builder view.
 * Full implementation deferred to Cycle 3.
 *
 * See: TASK-019, ux-spec.md — Pipeline Builder
 */

import React from 'react'

/**
 * PipelineManagerPage renders a placeholder until the Pipeline Builder is implemented.
 */
function PipelineManagerPage(): React.ReactElement {
  return (
    <div>
      <h1 style={{ fontSize: '20px', fontWeight: 600, color: 'var(--color-text-primary)' }}>
        Pipeline Builder
      </h1>
      <p style={{ color: 'var(--color-text-secondary)', marginTop: '8px' }}>
        Coming in Cycle 3.
      </p>
    </div>
  )
}

export default PipelineManagerPage
